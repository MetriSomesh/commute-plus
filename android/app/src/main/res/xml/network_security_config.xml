<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext (HTTP) traffic ONLY to local development backends:
    - 10.0.2.2  : the host machine as seen from the Android emulator
    - localhost : on-device loopback
  All other traffic remains HTTPS-only (the platform default on API 28+).
  For a production build, point the app at an HTTPS backend and remove these exceptions.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
