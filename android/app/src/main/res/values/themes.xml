<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme for the non-Compose parts (splash, manifest). -->
    <style name="Theme.CommutePlus" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
